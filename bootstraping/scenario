assemble {
  # Run the 'assemble' task
tasks = ["clean"]
}
clean_build {
versions = ["5.1"]
tasks = ["assembleDebug"]
gradle-args = ["--parallel"]

cleanup-tasks = ["clean"]
run-using = cli



warm-ups = 20
}
